# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# src/version.ts is committed as a package.json reader; build.sh overwrites it
# with a constant for --compile, then restores the source (do not commit that form).

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store
.codereview/
.claude/settings.local.json
